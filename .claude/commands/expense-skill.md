Load the expense skill context from `.claude/skills/expense-skill/expense-skill.md` and keep it active for this conversation.

You now have full context about the Expense feature. Apply it when answering any task related to:
- CaptureScreen (camera, preview phase)
- Photo upload to Cloudinary via signed URL
- useCreateExpense, useUploadImage hooks
- AmountInput, CapturePreviewPhase, CaptureTopBar components
- Backend /expenses and /storage/upload-url APIs
- Expense entity schema and DTOs

Confirm with: "✦ Expense skill loaded — sẵn sàng." then wait for user's task.
