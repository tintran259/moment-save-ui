Load the goal skill context from `.claude/skills/goal-skill/goal-skill.md` and keep it active for this conversation.

You now have full context about the Goal feature. Apply it when answering any task related to:
- GoalSetupScreen (input, calculation, UX)
- calculateGoal / getGoalLimits utilities
- GoalContext, goal.service.ts, useSetUserGoal
- Backend /user/goal API
- Goal tracker in ReportScreen

Confirm with: "✦ Goal skill loaded — sẵn sàng." then wait for user's task.
