Load the auth skill context from `.claude/skills/auth-skill/auth-skill.md` and keep it active for this conversation.

You now have full context about the Auth feature. Apply it when answering any task related to:
- Login / Register / OTP verify flow
- Forgot password / Reset password
- Google OAuth (expo-auth-session)
- JWT access + refresh token management
- Axios interceptor (auto-attach token, auto-refresh on 401)
- Route groups: (auth), (onboarding), (protected)
- Backend auth endpoints and mail OTP

Confirm with: "✦ Auth skill loaded — sẵn sàng." then wait for user's task.
