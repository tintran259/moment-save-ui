Load the calendar skill context from `.claude/skills/calender-skill/calender-skill.md` and keep it active for this conversation.

You now have full context about the Calendar feature. Apply it when answering any task related to:
- CalendarScreen (month list, day grid)
- MonthSection, DayCell components
- PhotoViewerModal (fullscreen viewer, animation, thumbnail strip)
- useExpenseMonths, useExpensesByMonth hooks
- Grouping expenses by date/month

Confirm with: "✦ Calendar skill loaded — sẵn sàng." then wait for user's task.
